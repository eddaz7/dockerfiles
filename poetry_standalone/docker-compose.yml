version: "3"
services:
  app:
    # if you want to download built image uncomment line 5 and comment line 6
    # image: "eddaz/poetry_standalone"
    build: .
    volumes:
      - ./workspace:/root/workspace
    container_name: poetry_standalone
    tty: true
